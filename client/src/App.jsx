// Root component. Route trees (storefront / artisan / admin) are wired in
// a later phase — this is placeholder scaffold only.
export default function App() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-cream text-navy">
      <h1 className="text-4xl font-display">VIRASAT</h1>
    </main>
  );
}
